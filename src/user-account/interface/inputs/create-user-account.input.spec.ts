import { CreateUserAccountInput } from './create-user-account.input';

describe('CreateUserAccountInput', () => {
  it('should be defined', () => {
    expect(new CreateUserAccountInput()).toBeDefined();
  });
});
