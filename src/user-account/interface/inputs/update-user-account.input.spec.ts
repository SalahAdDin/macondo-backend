import { UpdateUserAccountInput } from './update-user-account.input';

describe('UpdateUserAccountInput', () => {
  it('should be defined', () => {
    expect(new UpdateUserAccountInput()).toBeDefined();
  });
});
